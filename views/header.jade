header    
  .container
    .row-fluid
      .span4
        h1
          a(href="/") HackDash
        .description 
          p.lead
            span A Hackaton Dashboard 
            if(!user || user.is_social)
              span#slogan for people
            else
              span(style="color: black; display: inline;") for #{user.company}
      .span5
      .span3
        .createProject
         .btn-group
          if(user)
            a.btn.btn-large.btn-custom(href="/projects/create").tooltips(rel='tooltip', data-placement='bottom', data-original-title='Create a new project') New Project
            a.btn.btn-large.btn-profile(href="/users/profile")
             img.avatar(src=user.picture + '?s=50').tooltips(rel='tooltip', data-placement='bottom', data-original-title='Edit profile')
            a.btn.btn-large.btn-cancel(href="/logout").tooltips(rel='tooltip', data-placement='bottom', data-original-title='Log out from Hackdash') Logout
          else
            a.btn.btn-large.btn-custom(href="/login") Log in to create a project
            a.btn.btn-large.btn-custom(href='/prov', style="background-color: rgb(51, 151, 160)") Create a company hack-dashboard
